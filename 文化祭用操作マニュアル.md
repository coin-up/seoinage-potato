# 谷口の背負い投げポテト
## 文化祭用 受付管理システム操作マニュアル

**対象:** 文化祭当日の受付・調理・受け渡し担当スタッフ

このシステムは、QRコードから入場して、チケット番号の受付状況を共有するためのWebアプリです。購入者の受付登録と、スタッフの調理・受け渡し管理を同じデータベースで扱います。画面を閉じてもデータは保存され、複数の端末で受付状況が同期されます。

## 1. 使用する入口

各入口は、原則として配布済みのQRコードをスマートフォンのカメラで読み取って開いてください。機能ページのURLを直接入力した場合は利用できません。

| 担当・用途 | QR入口URL |
|---|---|
| 参加者・購入者受付 | https://seoinagepota-fbjzzh6l.manus.space/buyer-only-x5k9m2a7q8r3 |
| 管理・受付両用 | https://seoinagepota-fbjzzh6l.manus.space/buyer-a7k9m2x5q8r3 |
| 管理者専用 | https://seoinagepota-fbjzzh6l.manus.space/admin-b4n6p1j9w2e8 |

**重要:** 管理者用QRと両用QRはスタッフだけで管理してください。QR入口を読み取ると、対応するページへ移動します。直接 `/buyer`、`/admin`、`/combined` を開いても機能は表示されません。

## 2. 参加者・購入者の受付手順

参加者には、まずページ上部の注意事項と受け取り手順を読んでもらいます。注文は**来店の10～15分前**を目安に行います。揚げる時間により多少前後する場合があるため、参加者にはあらかじめ了承してもらってください。

注意事項を確認したら、ページ下部の「チケット番号を入力」欄へ、QRチケットに記載された**英字1文字＋3桁番号**を入力します。例えば `A123` のように入力してください。小文字で入力しても自動的に大文字へ変換されますが、4文字以外の入力や数字の桁数が違う入力は受付できません。

入力内容を確認して「受付する」を押します。受付が成功すると完了メッセージが表示され、管理画面へチケット番号と受付時刻が送られます。受付後はチケット番号を変更できないため、送信前に必ず確認してください。同じチケット番号を再度受付しようとした場合は、重複受付としてエラーになります。

## 3. 管理者ページの使い方

調理担当または受け渡し担当は、管理者用QRを読み取って管理者ページを開きます。画面には「未対応」と「対応済み」の2つの一覧が表示されます。

「未対応」には、受付時刻が古いチケットから上に並びます。番号の横に表示される時刻を見ながら、古い受付から順に調理してください。番号の一部を検索欄へ入力すると、該当するチケットだけに絞り込めます。例えば `A1` と入力すると、`A1` を含むチケットを検索できます。

商品を渡す準備ができ、参加者へチケット番号を呼び出して商品を渡したら、その行の「対応済みにする」ボタンを押します。対象のチケットは未対応一覧から対応済み一覧へ移動します。押し間違えを防ぐため、受け渡しが完了してからボタンを押してください。

登録、対応済みへの移動、検索結果は、画面を開いている別の端末にも自動的に反映されます。反映に時間がかかる場合は、画面を再読み込みして一覧を確認してください。

## 4. 管理・受付両用ページの使い方

受付担当と管理担当を同じ端末で行う場合は、管理・受付両用QRを使用します。このページでは、参加者のチケット受付、未対応一覧の確認、検索、対応済みへの移動を一画面で行えます。

通常は、左側の受付フォームでチケット番号を登録し、右側の未対応一覧から調理・受け渡し状況を確認します。対応済みへの移動方法は管理者ページと同じです。

## 5. 全データを初期化する方法

次の時間帯の受付を新しく始めるなど、管理上の理由で全件を削除する場合だけ、両用ページの「全データを初期化」を使用します。

ボタンを押すと確認ダイアログが表示されます。未対応と対応済みの**すべての受付データが削除され、元に戻せません**。担当責任者が内容を確認したうえで、「初期化する」を押してください。迷った場合は「キャンセル」を押し、初期化しないでください。

## 6. 当日の役割分担の例

| 役割 | 推奨ページ | 主な操作 |
|---|---|---|
| 参加者 | 購入者受付 | 注意事項を確認し、チケット番号を受付登録 |
| 受付担当 | 両用ページ | 参加者の入力補助、受付状況の確認 |
| 調理担当 | 管理者ページ | 未対応を古い順に確認し、調理状況を共有 |
| 受け渡し担当 | 管理者ページ | 番号を呼び、商品を渡して対応済みに変更 |
| 責任者 | 両用ページ | 必要な場合だけ全データ初期化を実行 |

## 7. 困ったときの確認手順

受付できない場合は、チケット番号が「英字1文字＋3桁番号」になっているかを確認します。すでに受付済みの番号でないかも確認してください。

管理画面に新しい受付が表示されない場合は、通信状態を確認し、数秒待ってから再読み込みします。別端末にも表示されない場合は、参加者側で受付完了メッセージが表示されたかを確認してください。

QRを読み取っても受付画面が表示されない場合は、カメラでQRをもう一度読み取り直してください。URLを直接入力している場合は、配布されたQRコードから入り直します。

操作に迷った場合や、初期化を行うべきか判断できない場合は、独断で初期化せず、責任者へ確認してください。

## 8. 開始前チェックリスト

| 確認項目 | 確認 |
|---|---|
| 参加者用・管理者用・両用のQRコードをそれぞれ読み取れる | □ |
| 管理者ページで未対応・対応済み一覧が表示される | □ |
| テスト用チケット番号で受付登録から対応済み移動まで確認した | □ |
| 受付担当と管理担当の端末を別々に用意した | □ |
| 端末の充電と通信状態を確認した | □ |
| 全データ初期化の実行担当者を決めた | □ |
| 文化祭開始前にテストデータを初期化した | □ |

文化祭終了後や時間帯の切り替え時に初期化する場合は、未対応の注文が残っていないことを必ず確認してください。
