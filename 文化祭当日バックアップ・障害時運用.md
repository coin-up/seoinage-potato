# 谷口の背負い投げポテト
## 文化祭当日 バックアップ・障害時運用手順

**対象:** 文化祭当日の責任者、受付担当、管理担当

この手順書は、受付データの消失や通信障害が起きた場合に、注文を重複させず、受け渡し状況を安全に復元するためのものです。通常時の画面操作は、同梱の「文化祭用操作マニュアル.md」を使用してください。

## 1. 基本原則

本システムの注文データはMySQLに保存されます。画面の「全データを初期化」はバックアップではなく、未対応・対応済みを含む全件削除です。責任者の確認なしに押してはいけません。

障害発生中は、同じチケット番号を複数の端末から再送信しないでください。受付済みか判断できない番号は、画面が復旧するまで紙の「保留・確認リスト」に記録し、商品を二重に作らないようにします。

## 2. 開始前に行うバックアップ

本番開始前に、責任者は次の項目を確認します。

| 手順 | 実施内容 | 完了確認 |
|---|---|---|
| 1 | RailwayまたはRenderのDB管理画面を開き、対象のMySQLサービスを確認する | □ |
| 2 | DBのスナップショット、バックアップ、またはエクスポート機能でバックアップを作成する | □ |
| 3 | 作成日時と対象DB名を、責任者だけが見られる記録へ保存する | □ |
| 4 | 復元先と復元担当者を決め、文化祭開始前に連絡方法を確認する | □ |
| 5 | アプリへ入り、テスト番号を1件だけ登録・対応済みにして動作確認する | □ |
| 6 | テスト番号を両用ページから初期化し、未対応・対応済みが空であることを確認する | □ |

プロバイダーのバックアップ機能名や保存期間は契約・プランにより異なります。画面に「Backup」「Snapshot」「Export」などの項目がない場合は、管理画面の公式手順に従ってSQLエクスポートを行い、ダウンロードしたファイルを学校・店舗の責任者が管理する安全な場所へ保存してください。GitHubリポジトリや公開チャットへバックアップファイルを置いてはいけません。

## 3. 通常運用中の記録

受付担当は、画面に受付完了が表示された番号を必要に応じて紙の控えにも記録します。特に通信が不安定な時間帯、画面を再読み込みした直後、担当者を交代する直前は、最後に確認した未対応番号と対応済み番号を簡単に引き継いでください。

「受付完了」と表示されたのに管理画面へ出ない場合は、すぐに再送信せず、次の項目を紙へ記録します。

| 記録項目 | 記入内容 |
|---|---|
| 時刻 | 受付を試した時刻 |
| チケット番号 | 例: A123 |
| 受付者 | 参加者または担当者の識別 |
| 画面表示 | 完了、通信エラー、画面不明など |
| 受け渡し | 未確認、調理前、受け渡し済みのいずれか |

## 4. 障害の切り分け

まず、受付担当と管理担当の両方で通信状態を確認します。片方の端末だけで表示されない場合は、その端末の再読み込み、Wi-Fi／モバイル通信の切り替え、QR入口からの再入場を順に試します。複数端末で同時にAPIへ接続できない場合は、アプリまたはDB側の障害の可能性があります。

SSEの更新だけが止まっている場合でも、自動再取得または画面の再読み込みで一覧が更新されることがあります。再読み込み後も受付と管理の両方が確認できない場合は、責任者が「障害中」と判断し、紙運用へ切り替えます。

## 5. 障害中の受付・調理・受け渡し

障害中は、受付担当を一人に固定し、紙の保留・確認リストを一冊だけ使います。参加者からチケット番号を受け取り、時刻と番号を記録します。番号の読み間違いを防ぐため、参加者本人に番号を読み上げてもらい、担当者が復唱します。

調理担当は、紙リストに記録された番号だけを調理します。すでに調理した番号には「調理済み」、商品を渡した番号には「受け渡し済み」と時刻を記入します。判断できない番号は調理・受け渡しを保留し、責任者へ確認します。

障害中にアプリへ再接続できるようになった場合も、紙リストの番号を一括で再送信しないでください。責任者と受付担当が画面上の既存データと紙リストを突き合わせ、未登録の番号だけを1件ずつ登録します。登録済みまたは状態不明の番号は、管理画面で確認してから対応済みにします。

## 6. 復旧後の照合

復旧後は、責任者、受付担当、管理担当の3者で紙リストと管理画面を照合します。照合が終わるまで「全データを初期化」を実行してはいけません。

| 照合結果 | 対応 |
|---|---|
| 画面にも紙にもあり、未対応 | 通常どおり調理・受け渡しを続ける |
| 画面にも紙にもあり、対応済み | 紙に対応済み時刻を追記し、二重提供しない |
| 紙にだけある | 画面へ1件ずつ登録し、重複エラーが出たら既存データを確認する |
| 画面にだけある | 紙の担当者へ確認し、番号と状態を責任者が判定する |
| 番号または状態が不明 | 参加者へ確認し、責任者が調理・受け渡しを決定する |

照合後、責任者は紙リストの最終行、未対応件数、対応済み件数を記録します。記録は次のバックアップ取得時まで安全に保管します。

## 7. 復旧・初期化の判断

DBの復元は、責任者がデータ消失や破損を確認し、復元時点を決めた場合だけ実施します。復元前には、現在のDB状態を別名で保存できる場合は保存し、復元後にアプリへ接続できることを確認します。復元で直近の受付が失われる可能性があるため、紙リストとの照合を必ず行います。

時間帯を切り替えるための全データ初期化は、未対応がないこと、受け渡し確認が終わっていること、紙リストとの照合が終わっていることを3点確認してから行います。初期化後は、管理画面を再読み込みして未対応・対応済みが空であることを責任者が確認します。

## 8. 連絡テンプレート

障害時は、担当者ごとに別の判断をしないよう、次の内容を責任者へ伝えます。

> 「時刻○時○分、参加者用／管理者用／両用の○台で、画面表示または受付登録ができません。最後に画面で確認できた番号は○○○○、紙リストの未照合件数は○件です。現在は紙運用へ切り替え、再送信は止めています。」

復旧時は、次の内容を共有します。

> 「時刻○時○分に画面へ再接続できました。紙リスト○件と画面を照合し、未登録○件、登録済み○件、状態確認中○件です。責任者確認後、未登録分だけを1件ずつ反映します。」

## 9. 文化祭終了後

文化祭終了後は、最終の未対応件数と対応済み件数を記録し、必要なバックアップまたはエクスポートを保存します。公開URL、管理者QR、DBバックアップを不要な相手へ共有しないでください。次回利用しない場合は、責任者が保存期間と削除方針を決めてから、ホスティングサービス上のデータを扱います。
